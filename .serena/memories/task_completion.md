### Task Completion
- **Verify Changes**: Always check `chezmoi diff` before confirming changes or applying.
- **Testing**: Run `python3 dot_config/opencode/skills/skill-creator/scripts/executable_quick_validate.py <skill-folder-path>` if modifying skills.
- **Secrets**: Ensure Bitwarden secrets are correctly referenced if adding new tools that require auth.
